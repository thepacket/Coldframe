{
  "name": "coldframe",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "A browser for one genomic locus across a thousand wild plants, ordered by the climate each came from.",
  "license": "MIT",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.9.3",
    "vite": "^7.1.9"
  }
}
